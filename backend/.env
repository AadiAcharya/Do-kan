PORT=3001
MONGO_URI=mongodb://localhost:27017/dokan
JWT_SECRET=dokan_dev_secret_key
JWT_EXPIRES=7d
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# eSewa Sandbox (test credentials — replace with live for production)
ESEWA_SECRET_KEY=8gBm/:&EnhH.1/q
ESEWA_PRODUCT_CODE=EPAYTEST
ESEWA_GATEWAY_URL=https://rc-epay.esewa.com.np/api/epay/main/v2/form
ESEWA_VERIFY_URL=https://rc-epay.esewa.com.np/api/epay/transaction/status/

# Khalti Sandbox (test credentials — replace with live for production)
KHALTI_SECRET_KEY=test_secret_key_dc74e0fd57cb46cd93832aee0a390234
KHALTI_GATEWAY_URL=https://dev.khalti.com/api/v2/epayment/initiate/
KHALTI_LOOKUP_URL=https://dev.khalti.com/api/v2/epayment/lookup/

# ─── EMAIL CONFIGURATION ──────────────────────────────────────────────────────
# Option 1: Gmail (requires an App Password, not your normal Gmail password)
# Generate one at https://myaccount.google.com/apppasswords
EMAIL_SERVICE=gmail
EMAIL_USER=newarsachin07@gmail.com
EMAIL_PASS=bmix vmpl clzs cgro

# Option 2: Generic SMTP (e.g. Mailtrap for testing, SendGrid, etc.)
# Leave EMAIL_SERVICE blank and uncomment below to use raw SMTP instead
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_pass

# If EMAIL_USER and EMAIL_PASS are left blank, emails are skipped silently
# and logged to the console — the app will still work without email configured.
